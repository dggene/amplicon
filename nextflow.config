docker.enabled = true

process{
    tag={ sample_name }
    containerOptions='--volume /DG/database:/DG/database --volume /DG/rawdata:/DG/rawdata --volume /DG/programs:/DG/programs --volume /DG/project0:/DG/project0'
    echo = true
}

report.enabled=true

mail {
    from = 'system@decodegenomics.com'
    smtp.host = 'smtp.decodegenomics.com'
    smtp.port = 25
    smtp.user = 'system@decodegenomics.com'
    smtp.password = 'T8SFuUg2'
    smtp.auth = true
}